# CSVキーワードテスト結果（2026-02-07）

## 目的
`faq_kb.csv` のキーワードが想定通りに回答へ反映されることを検証する。

## テスト方法
- CSVの `keywords` から先頭トークンを抽出し、質問として投げる。
- 期待する `intent` と、返却された `evidence` の先頭が一致するかを確認。
- キーワードは `|` 区切りを採用し、空白/`|` 両対応で分割。

## 結果概要
- **全件OK（12/12）**
- すべての質問で `evidence` が期待 `intent` と一致

## 詳細結果（抜粋）
- `水` → `設備_水漏れ`（OK）
- `鍵` → `鍵_紛失`（OK）
- `ペット` → `ペット飼育の可否`（OK）
- `故障` → `設備_故障`（OK）
- `ゴミ` → `ゴミ出し_ルール`（OK）
- `インターネット` → `生活_インターネット`（OK）
- `ガス` → `生活_ガス`（OK）
- `電気` → `生活_電気`（OK）
- `退去` → `契約_退去解約`（OK）
- `証明書` → `契約_証明書`（OK）
- `原状回復` → `契約_原状回復`（OK）
- `喫煙` → `規則_喫煙禁止`（OK）

## ログ確認ポイント
- キーワード一致時に以下ログが出力されることを確認:
  - `[INFO] CSV keyword match detected. Skipping score threshold (matched intent: <intent>)`

