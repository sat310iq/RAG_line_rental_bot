# Cloud Build config for line-worker (uses deploy/Dockerfile.worker).
# Called by deploy_worker.sh — do not invoke directly.
# Prereq: gcloud artifacts repositories create cloud-run --repository-format=docker --location=asia-northeast1
substitutions:
  _IMAGE_NAME: 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run/line-worker'
steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '$_IMAGE_NAME', '-f', 'deploy/Dockerfile.worker', '.']
images:
  - '$_IMAGE_NAME'
